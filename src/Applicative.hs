module Applicative
  ( -- * Applicative
    Applicative(..),
    filterM,
    forever,
    liftA3,
    replicateM,
    replicateM_,
    unless,
    when,
    zipWithM,
    zipWithM_,
  ) where

import Control.Applicative
import Control.Monad
