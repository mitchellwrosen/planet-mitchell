module Optic.Prism
  ( -- * Prism
    Prism,
    Prism',
    prism,
    prism',
    is,
    only,
  ) where

import Control.Lens.Extras (is)
import Control.Lens.Prism (Prism, Prism', prism, prism', only)
